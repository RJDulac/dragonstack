//global units of time
const SECONDS = 1000;
const Minutes = SECONDS * 60;
const HOURS = Minutes * 60;
const DAYS = HOURS * 24;

const REFRESH_RATE = 5; //units

module.exports = {
    SECONDS,
    Minutes,
    HOURS,
    DAYS,
    REFRESH_RATE
};

